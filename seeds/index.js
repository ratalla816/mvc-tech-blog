const seedUsers=require("./user-seeds"),seedPosts=require("./post-seeds"),seedComments=require("./comment-seeds"),sequelize=require("../config/connection"),seedAll=async()=>{await sequelize.sync({force:!0}),console.log("--------------"),await seedUsers(),console.log("--------------"),await seedPosts(),console.log("--------------"),await seedComments(),console.log("--------------"),process.exit(0)};seedAll();